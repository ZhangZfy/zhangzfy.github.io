<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://zhangzfy.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://zhangzfy.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-07T04:04:25+00:00</updated><id>https://zhangzfy.github.io/feed.xml</id><title type="html">Zhengfeiyang (Finn) Zhang</title><subtitle># Personal website of Zhengfeiyang Finn Zhang. </subtitle><entry><title type="html">reproduction: MiniOneRec</title><link href="https://zhangzfy.github.io/blog/2026/minionerec/" rel="alternate" type="text/html" title="reproduction: MiniOneRec"/><published>2026-05-07T00:00:00+00:00</published><updated>2026-05-07T00:00:00+00:00</updated><id>https://zhangzfy.github.io/blog/2026/minionerec</id><content type="html" xml:base="https://zhangzfy.github.io/blog/2026/minionerec/"><![CDATA[<h2 id="minionerec">MiniOneRec</h2> <p><strong>MiniOneRec</strong> 是第一个开源的生成式推荐系统框架。它成功地将 LLMs 应用于推荐系统，在公开数据集上验证了推荐系统也可以获益于 scaling law。整个训练过程由 Semantic Encoding，SFT， GRPO 三部分构成，三个阶段环环相扣：编码层提供结构化的商品表示，SFT 建立基础的推荐能力与语义对齐，GRPO 则在此基础上进一步优化排序质量和候选多样性。</p> <p>在此，不能不提及快手在 2025 年发布的 <strong>OneRec</strong>，OneRec 是快手为亿级用户和视频多模态内容设计的完整工业系统，在多项推荐任务相关指标上取得了非常显著的提升，并且已经被正式部署在快手的多个应用上。 <a href="https://arxiv.org/pdf/2506.13695">OneRec Technical Report</a> 详细介绍了 OneRec 的架构设计和实验结果，但并未对源代码和数据集进行开源，且对算力的要求巨大，因此个人很难进行复现。在此背景下，MiniOneRec 随即诞生，它开源了训练数据集和模型框架，验证了生成式推荐系统确实能获益于 scaling law。</p> <p>但必须指出，MiniOneRec 是面向学术开源场景的简化实现，其定位是”Mini”。在整个架构设计上，它和 OneRec 框架存在差异，并非对 OneRec 的完整复现。但是，我们仍然可以通过 MiniOneRec 了解生成式推荐系统的设计哲学，并且这一整套从训练 RQ-VAE 编码器、扩充 LLM tokenizer，到对 LLM 做 SFT、GRPO 的全流程，能够帮助我们熟悉大模型微调相关的各项技术。</p> <hr/> <h2 id="模型架构">模型架构</h2> <p><img src="/assets/img/posts/2026-05-07-minionerec/minionerec_overview.png" alt=""/></p> <p>MiniOneRec 的整体流程可以分为三个阶段：</p> <ol> <li> <p>商品编码（左上角） Item info 经过 LLM Encoder 生成语义 Embedding，再经 RQ-VAE 量化为三个离散 token（a、b、c）组成的 Item SID，构建商品的结构化表示。</p> </li> <li> <p>LLM 推理与生成（左侧主体） 以用户的 SID 历史序列作为输入，由一个 Decoder-only LLM 自回归地预测下一个商品的 SID在 RL 阶段，通过 Constrained Beam Search 同时生成 G 个不同的候选商品，并保证每个输出都是合法的 Valid SID。</p> </li> <li> <p>奖励计算与 GRPO 更新（右侧） 对 G 个候选商品分别计算两种奖励并相加：</p> <ul> <li>ACC Reward：二值奖励，命中目标商品得 1 分，否则 0 分</li> <li>Rank Reward：排序感知奖励，对排在前面的错误负样本施加更强惩罚 两者之和送入 GRPO 进行策略梯度更新。</li> </ul> </li> </ol> <p>值得注意的是，训练时保持贯穿全程的 Full-Process SID Alignment，即 SID 与自然语言之间的对齐任务在 SFT 和 RL 两个阶段始终保持，确保 LLM 的世界知识持续与 SID 空间绑定，而非只在某一阶段对齐。</p> <h3 id="商品编码">商品编码</h3> <p>在传统推荐系统中，每件商品直接对应一个随机初始化的 Embedding 向量，模型通过用户-商品交互信号来学习这个向量的值，商品的语义内容（标题、描述等）往往是辅助特征，而非主体。这种设计的问题在于：Embedding 表纯粹依赖协同过滤信号，无法捕捉商品间的语义相似性，且参数量随商品数量线性增长，难以扩展，更无法享受 LLM 的 scaling 红利。</p> <p>生成式推荐的核心思路转变在于：<strong>让语言模型直接”说出”下一个商品是什么</strong>。但语言模型只能生成 token 序列，无法直接输出一个商品 ID，因此必须先把每件商品压缩成一段离散的 token 序列（即 SID），让语言模型的生成空间与商品空间对齐。商品编码做的正是这件事——它是连接”商品世界”与”语言模型生成空间”的桥梁，没有它，自回归生成推荐就无从谈起。</p> <p>MiniOneRec 选择了 RQ-VAE 作为编码器。 <a href="https://arxiv.org/pdf/2305.05065">TIGER</a> 第一次把 RQ-VAE 引入推荐系统，RQ-VAE 之所以被选用，是因为它能将语义向量以层次化、紧凑的方式压缩为少量离散 token，既保留了语义信息，又控制了词表规模，使得语言模型的生成是可行且高效的。</p> <p>下面，详细解释一下 MiniOneRec 中 RQ-VAE 的训练过程：</p> <ul> <li>第一步：文本输入 对每件商品 $i$，将其标题和描述拼接为一段文字，作为编码的原始输入。</li> <li>第二步：语义向量提取 将拼接后的文本送入冻结的文本编码器（MiniOneRec 中使用 Qwen3-Embedding-4B），输出 $d$ 维语义向量： $x \in \mathbb{R}^d$ ，编码器权重在训练中保持不变。</li> <li>第三步：残差量化（核心） RQ-VAE 的量化过程共 $L=3$ 层，每层有一个独立的码本 $\mathcal{C}_l$，每个码本含 $K=256$ 个码字。 <strong>初始化</strong>：令残差 $r_0 = x$ 对每一层 $l$（$l = 0, 1, 2$），执行： <ol> <li> <p><strong>查找最近码字</strong>：在码本 $\mathcal{C}_l$ 中找到与当前残差 $r_l$ 距离最近的码字索引：</p> \[c_l = \arg\min_k \|r_l - e_k^{(l)}\|_2\] </li> <li> <p><strong>更新残差</strong>：用当前残差减去选中的码字向量，得到下一层输入：</p> <p>\(r_{l+1} = r_l - e_{c_l}^{(l)}\) 三层完成后，商品 $i$ 被表示为三个离散索引 $(c_0, c_1, c_2)$，即其 <strong>SID</strong>。码本大小为 $256^3 = 2^{24}$，足以覆盖数亿量级的商品目录。</p> </li> </ol> </li> <li> <p>第四步：重建与训练 量化后的潜变量通过各层码字向量相加恢复：</p> \[z_q = \sum_{l=0}^{L-1} e_{c_l}^{(l)}\] <p>再经解码器 $D(\cdot)$ 重建原始向量：</p> \[\hat{x} = D(z_q)\] <p>训练损失由重建损失与 RQ 正则项组成：</p> \[\mathcal{L}(x) = \underbrace{\|x - \hat{x}\|_2^2}_{\text{重建损失}} + \underbrace{\sum_{l=0}^{L-1}\left(\|\text{sg}[r_l] - e_{c_l}^{(l)}\|_2^2 + \beta\|r_l - \text{sg}[e_{c_l}^{(l)}]\|_2^2\right)}_{\text{RQ 正则项}}\] <p>其中 $\text{sg}[\cdot]$ 为 stop-gradient 算子，$\beta$ 控制 commitment loss 强度。正则项驱使码字向残差靠拢（第一项），同时让残差向码字靠拢（第二项），防止编码器输出漂移过远。</p> </li> </ul> <p>从直觉上理解， 可以把 RQ-VAE 类比为一种”粗到细”的近似：第一层码字负责捕捉语义的主要方向，后续层逐步修正前一层留下的误差，三层合力才完整描述商品语义。这也是为什么 SID 是三个 token 的层次结构，而非一个扁平的单一 ID。</p> <h3 id="sft">SFT</h3> <p>SFT 阶段的核心目的有两个：一是让 LLM 学会根据用户历史序列预测下一个商品的 SID（推荐能力）；二是将 LLM 内部的世界知识与 SID 空间建立双向映射（对齐能力）。两者联合训练，共同构成 RL 阶段的起点。</p> <p>在进入训练之前，需要将 RQ-VAE 生成的所有 SID token 添加到 LLM 原始词表中。具体做法是追加一个三层码本，每层 256 个专用 token，这些 token 被视为<strong>不可分割的原子单元</strong>，不会被分词器拆成子词。这一步是对齐的物质基础——LLM 必须先”认识”SID，才能对其进行读写。</p> <p>SFT 阶段包含两大组任务，<strong>同时混合训练</strong>：</p> <ol> <li> <p>推荐任务（Recommendation Tasks）</p> <p><strong>① 生成式检索（主任务）</strong> 输入用户按时间排序的 SID 交互历史，指令为”推荐下一个商品”，模型输出下一个商品的 SID。这是推荐系统的核心任务，直接对应最终评估指标。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> Input:  用户历史 &lt;a_13&gt;&lt;b_197&gt;&lt;c_1&gt;, &lt;a_52&gt;&lt;b_17&gt;&lt;c_113&gt;, ...
 Can you predict the next possible item?
 Output: &lt;a_13&gt;&lt;b_72&gt;&lt;c_149&gt;
</code></pre></div> </div> <p><strong>② 非对称商品预测</strong> 两个互补的子任务，强制模型在文本空间和 SID 空间之间切换：</p> <ul> <li>给定<strong>文本形式</strong>的历史（商品标题列表），预测下一个商品的 <strong>SID</strong></li> <li>给定<strong>SID 形式</strong>的历史，预测下一个商品的<strong>文本标题</strong> 这两个任务的意义在于：迫使模型建立”同一商品的两种表达”之间的等价关系，而不是把 SID 当作与文本完全无关的符号。</li> </ul> </li> <li> <p>对齐任务（Alignment Tasks）</p> <p><strong>① SID–文本语义对齐（双向）</strong></p> <ul> <li>给定一个 SID，预测对应商品的文本标题</li> <li>给定一个文本标题，预测对应商品的 SID</li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> Input:  What is the title of &lt;a_24&gt;&lt;b_141&gt;&lt;c_73&gt;?
 Output: Oral-B Deep Sweep Toothbrush
	
 Input:  Which item has the title "Nashua Stretch &amp; Seal Self-Fusing Silicone Tape"?
 Output: &lt;a_202&gt;&lt;b_202&gt;&lt;c_29&gt;
</code></pre></div> </div> <p><strong>② 商品描述重建</strong> 给定一段商品描述，预测对应 SID；反之给定 SID，生成商品描述。由于描述空间开放且多样，该任务仅在 SFT 阶段使用，不延续到 RL 阶段。</p> <p><strong>③ 用户偏好摘要</strong></p> <p>给定一段用户的 SID 交互历史，生成一段自然语言描述该用户的兴趣偏好。由于数据集中没有现成的偏好标签，论文使用 DeepSeek 从商品元数据和用户评论中抽取伪标签作为监督信号。同样<strong>仅限于 SFT 阶段</strong>。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> Input:  用户历史 &lt;a_39&gt;&lt;b_41&gt;&lt;c_1&gt;, &lt;a_39&gt;&lt;b_28&gt;&lt;c_16&gt;, ...
     Can you summarize the user's preference?
 Output: This user has a clear preference for durable, high-performance 
     practical supplies for hands-on tasks...
</code></pre></div> </div> </li> </ol> <h3 id="grpo">GRPO</h3> <p>SFT 阶段让模型学会了基本的推荐能力，但其优化目标是模仿训练数据的分布，并不直接优化排序质量。GRPO 阶段的目的是在 SFT checkpoint 基础上，通过强化学习进一步让模型学会区分好坏候选、提升排序精度和候选多样性。</p> <p>将 GRPO 用于推荐场景面临两个独特障碍，MiniOneRec 分别给出了针对性的设计：</p> <ul> <li> <p>挑战一：采样多样性不足</p> <p><strong>问题</strong>：推荐的动作空间是一个封闭的 SID 集合，规模远小于自然语言词表。对同一个 prompt 多次采样，模型往往反复生成相同的商品，导致候选中有大量重复，模型几乎观察不到有效的负样本，训练信号极度稀疏。</p> <p><strong>解决方案</strong>：Beam Search 结合<strong>Constrained Decoding</strong>：在每一步生成时，屏蔽所有不合法的 token，确保每条 beam 最终都能映射到一个真实存在的商品，从而保证奖励计算的可行性。</p> </li> <li> <p>挑战二：奖励信号稀疏</p> <p><strong>问题</strong>：标准 GRPO 使用二值奖励——命中目标商品得 1 分，其余全为 0。这种设计把所有负样本一视同仁，而实际上模型对不同负样本的置信度差异很大，高置信度的错误（即模型最倾向于推荐却是错的）应该受到更强的惩罚，否则排序质量难以提升。</p> <p><strong>解决方案：混合奖励</strong></p> <p>最终奖励由两部分相加构成：$R(e_k,e_t)=R_{rule}(e_k,e_t)+R_{rank}(e_k,e_t)$ ① 规则奖励（ACC Reward）：标准二值信号 \(R_{rule}(e_k,e_t)= \begin{cases} 1, &amp; e_k=e_t \\ 0, &amp; \mathrm{otherwise} \end{cases}\) ② 排序感知奖励（Rank Reward）：对生成概率排名靠前的负样本施加更强惩罚。设负样本 $e_k$​ 在模型输出中的概率排名为 $\rho_k$（$ρ$=1 表示最高概率）： \(\tilde{R}_{rank}(e_k,e_t)= \begin{cases} 0, &amp; e_k=e_t \\ -\frac{1}{\log(\rho_k+1)}, &amp; \mathrm{otherwise} \end{cases}\) 归一化后即为最终 $R_{rank}$。排名越靠前（$\rho_k$​ 越小），惩罚越大，迫使模型主动识别并压制那些”自信但错误”的推荐。</p> </li> </ul> <hr/> <h2 id="复现">复现</h2> <h3 id="rq-vae">RQ-VAE</h3> <p>严格按照 MiniOneRec 原始设置，使用 Qwen3-Embedding-4B 得到的文本嵌入作为原始数据进行训练，具体参数设置如下：</p> <table> <thead> <tr> <th>数据集</th> <th>Industrial_and_Scientific</th> </tr> </thead> <tbody> <tr> <td>总 epoch</td> <td>10,000</td> </tr> <tr> <td>Batch size</td> <td>20,480</td> </tr> <tr> <td>优化器</td> <td>AdamW，lr=1e-3，weight_decay=0</td> </tr> <tr> <td>LR scheduler</td> <td>constant（前 50 epoch warmup）</td> </tr> <tr> <td>损失函数</td> <td>MSE + commitment loss（β=0.25）+ per-level VQ loss</td> </tr> <tr> <td>quant_loss_weight</td> <td>1.0</td> </tr> <tr> <td>Codebook 结构</td> <td>3 级，每级 256 个码字，code dim=32</td> </tr> <tr> <td>MLP 层结构</td> <td>[2048, 1024, 512, 256, 128, 64]（encoder/decoder 对称）</td> </tr> <tr> <td>KMeans 初始化</td> <td>开启，100 次迭代</td> </tr> </tbody> </table> <p>训练后，选择最优检查点（epoch 8599）作为最终结果，结果如下：</p> <table> <thead> <tr> <th>指标</th> <th>值</th> </tr> </thead> <tbody> <tr> <td>Total loss</td> <td>0.2402</td> </tr> <tr> <td>Recon loss</td> <td>0.1096</td> </tr> <tr> <td>Level 0 VQ loss</td> <td>0.2590</td> </tr> <tr> <td>Level 1 VQ loss</td> <td>0.0859</td> </tr> <tr> <td>Level 2 VQ loss</td> <td>0.0472</td> </tr> <tr> <td>Collision rate</td> <td><strong>9.41%</strong>（220 个冲突组，347 个 item 非唯一）</td> </tr> <tr> <td>Codebook 利用率</td> <td>L0: 88/256，L1: 256/256，L2: 256/256</td> </tr> </tbody> </table> <p>根据原文设置，还需要进行碰撞处理，进行了 20 次 Sinkhorn 后处理后，碰撞率从 9.41% 下降到了 0.33%。</p> <p>下面对训练过程的损失函数、碰撞率和码本利用率可视化：</p> <ul> <li> <p>Loss 曲线：总 loss 从训练初期快速下降，在 2000 epoch 后趋于平稳，但有较大振荡（见图中毛刺）。Recon loss 在后期占主导。 <img src="/assets/img/posts/2026-05-07-minionerec/loss_curves.png" alt=""/></p> </li> <li> <p>Collision rate 曲线：从初始 ~1.0 急剧下降至 epoch ~2000 时的 ~0.15，之后缓慢改善至 10% 左右，epoch 8599 是 collision 最低点。 <img src="/assets/img/posts/2026-05-07-minionerec/collision_rate.png" alt=""/></p> </li> <li> <p>Codebook 利用率：L1、L2 层在约 4000 epoch 时达到近满利用（256/256），但 L0 层只用到 ~91/256 个码字（~35%），表明大多数 item 在第一级量化时聚到少量码字上，第二、三级再做细粒度区分。 <img src="/assets/img/posts/2026-05-07-minionerec/codebook_utilization.png" alt=""/></p> </li> </ul> <h3 id="sft-1">SFT</h3> <p><strong>训练的基础设置</strong>：</p> <table> <thead> <tr> <th>参数</th> <th>值</th> </tr> </thead> <tbody> <tr> <td>基座模型</td> <td>Qwen2.5-7B-Instruct</td> </tr> <tr> <td>GPU</td> <td>Quadro A6000 48GB x2</td> </tr> <tr> <td>数据集</td> <td>Industrial_and_Scientific</td> </tr> <tr> <td>总 batch size</td> <td>256（2 GPU DDP）</td> </tr> <tr> <td>优化器</td> <td>AdamW，bf16</td> </tr> <tr> <td>Warmup steps</td> <td>20</td> </tr> <tr> <td>Eval 频率</td> <td>每 5% epoch 一次</td> </tr> <tr> <td>Early stopping</td> <td>patience=3</td> </tr> <tr> <td>max_len</td> <td>512 tokens</td> </tr> <tr> <td>Beam search（推理）</td> <td>num_beams=50</td> </tr> </tbody> </table> <p>根据 MiniOneRec 在 Github 开源的训练脚本，实际训练过程中，并没有用到文中提到的所有 SFT 训练集，这里遵循 Github 脚本设置，SFT 训练时使用了如下几种数据集：</p> <ol> <li><code class="language-plaintext highlighter-rouge">SidSFTDataset</code> — 主任务：SID 历史序列 → 预测下一个 SID</li> <li><code class="language-plaintext highlighter-rouge">SidItemFeatDataset</code> — 对齐任务：SID ↔ 商品文本语义对齐</li> <li><code class="language-plaintext highlighter-rouge">FusionSeqRecDataset</code> — 辅助任务：标题历史序列 → 预测下一个 SID</li> </ol> <p>但验证集只用主任务（<code class="language-plaintext highlighter-rouge">SidSFTDataset</code>）。</p> <p><strong>训练配置及结果：</strong></p> <ul> <li>LoRA r=64，alpha=128，target 全部注意力+FFN 层</li> <li>lr=3e-4，batch_size=256，micro_batch_size=8</li> <li>Best eval loss <strong>1.5737</strong>，epoch <strong>3.0</strong>（early stopping）</li> </ul> <table> <thead> <tr> <th>指标</th> <th>值</th> <th>MiniOneRec 原文参考值</th> </tr> </thead> <tbody> <tr> <td>HR@1</td> <td>0.0735</td> <td>-</td> </tr> <tr> <td>HR@5</td> <td>0.1154</td> <td>0.1321</td> </tr> <tr> <td>HR@10</td> <td>0.1405</td> <td>0.1586</td> </tr> <tr> <td>HR@20</td> <td>0.1694</td> <td>-</td> </tr> <tr> <td>HR@50</td> <td>0.2305</td> <td>-</td> </tr> <tr> <td>NDCG@1</td> <td>0.0735</td> <td>-</td> </tr> <tr> <td>NDCG@5</td> <td>0.0953</td> <td>0.1084</td> </tr> <tr> <td>NDCG@10</td> <td>0.1035</td> <td>0.1167</td> </tr> <tr> <td>NDCG@20</td> <td>0.1108</td> <td>-</td> </tr> <tr> <td>NDCG@50</td> <td>0.1229</td> <td>-</td> </tr> </tbody> </table> <p>使用 LoRA 复现的结果略微劣于原文经过 SFT + GRPO 的完整模型，但优于原文给出的一系列 baseline ，这是一个比较合理的结果。</p> <p>附上原文中的 baseline 和 MiniOneRec 结果 <img src="/assets/img/posts/2026-05-07-minionerec/baselines.png" alt=""/></p> <h3 id="grpo-1">GRPO</h3> <p><strong>参数配置</strong>：</p> <table> <thead> <tr> <th>参数</th> <th>值</th> </tr> </thead> <tbody> <tr> <td>基座模型</td> <td>LoRA 合并后的 Qwen2.5-7B（SFT checkpoint）</td> </tr> <tr> <td>GPU</td> <td>Quadro A6000 48GB x2</td> </tr> <tr> <td>框架</td> <td>TRL <code class="language-plaintext highlighter-rouge">GRPOTrainer</code> （修改了一部分逻辑）</td> </tr> <tr> <td>精度</td> <td>bf16</td> </tr> <tr> <td>Epochs</td> <td>2</td> </tr> <tr> <td>lr</td> <td>1e-5（cosine scheduler，warmup_ratio=0.03）</td> </tr> <tr> <td>优化器</td> <td>paged_adamw_32bit</td> </tr> <tr> <td>per_device_train_batch_size</td> <td>64</td> </tr> <tr> <td>gradient_accumulation_steps</td> <td>128</td> </tr> <tr> <td>num_generations</td> <td>16（每条 prompt 采样 16 个候选答案）</td> </tr> <tr> <td>temperature</td> <td>1.0</td> </tr> <tr> <td>max_completion_length</td> <td>128</td> </tr> <tr> <td>beta</td> <td>1e-3（KL 散度惩罚系数）</td> </tr> </tbody> </table> <p><strong>训练数据</strong></p> <table> <thead> <tr> <th>数据集类</th> <th>任务</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">SidDataset</code></td> <td>主任务：SID 历史序列 → 预测下一个 SID</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">RLTitle2SidDataset</code></td> <td>对齐任务：商品标题 → SID（全量）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">RLSeqTitle2SidDataset</code></td> <td>辅助任务：标题历史序列 → 预测下一个 SID（采样 10000 条）</td> </tr> </tbody> </table> <p>验证集只用 <code class="language-plaintext highlighter-rouge">SidDataset</code>（主任务）。</p> <p><strong>训练结果</strong></p> <p>训练过程可视化：</p> <p><img src="/assets/img/posts/2026-05-07-minionerec/GRPO.png" alt=""/> 从图中可以观察到几个关键现象：</p> <ul> <li>Total Reward（左上：训练 reward 有轻微上升趋势（smoothed 曲线从 ~0.0012 升至 ~0.0030），但 eval 红点始终在低位（~0.0013），说明训练集上学到的东西在 eval 上转化有限。</li> <li>Rule vs NDCG Reward（右上）：rule_reward（绿）稳定正值 ~0.01-0.02，ndcg_reward（红）始终为负 ~-0.01，两者全程几乎没有变化——印证了训练中奖励信号极弱的问题。</li> <li>KL Divergence（左中）：从 0 单调上升到 ~20-30，模型持续偏离 ref model，但奖励没有跟上，说明策略在”探索”但学到的不一定对。</li> <li>Training Loss（左下）：注意 y 轴到了 100，出现了多个极端峰值——这是 KL 剧烈震荡时 penalty 项爆炸导致的，与 KL 曲线的尖峰对应。</li> <li>Eval Metrics vs Epoch（右下）：eval_reward 从 epoch 0.2 到 0.4 有一次小幅跳升，之后趋于平稳。整体改善幅度与最终 NDCG 提升（~4-6%）一致，只是代理指标不够直观。</li> </ul> <p>对 GRPO 训练后的模型重新在测试集上评估：</p> <table> <thead> <tr> <th>指标</th> <th>SFT lora_merged（起点）</th> <th>GRPO merged</th> <th>变化</th> </tr> </thead> <tbody> <tr> <td>NDCG@1</td> <td>0.0735</td> <td><strong>0.0774</strong></td> <td>+5.4%</td> </tr> <tr> <td>NDCG@5</td> <td>0.0953</td> <td><strong>0.0986</strong></td> <td>+3.5%</td> </tr> <tr> <td>NDCG@10</td> <td>0.1035</td> <td><strong>0.1081</strong></td> <td>+4.4%</td> </tr> <tr> <td>NDCG@20</td> <td>0.1108</td> <td><strong>0.1164</strong></td> <td>+5.0%</td> </tr> <tr> <td>HR@1</td> <td>0.0735</td> <td><strong>0.0774</strong></td> <td>+5.4%</td> </tr> <tr> <td>HR@5</td> <td>0.1154</td> <td><strong>0.1178</strong></td> <td>+2.1%</td> </tr> <tr> <td>HR@10</td> <td>0.1405</td> <td><strong>0.1469</strong></td> <td>+4.6%</td> </tr> <tr> <td>HR@20</td> <td>0.1694</td> <td><strong>0.1798</strong></td> <td>+6.1%</td> </tr> <tr> <td>HR@50</td> <td>0.2305</td> <td>0.2301</td> <td>≈0</td> </tr> </tbody> </table> <ul> <li>NDCG@1~@20 和 HR@1~@20 全部改善 2-6%，说明模型在 top-K 的排序质量上确实学到了东西</li> <li>HR@50 几乎持平（-0.2%），说明”召回覆盖率”没变，GRPO 的收益主要来自把正确答案推到更靠前的位置</li> <li>训练日志中奖励信号很弱，但 beam search 评估指标出现了一致性提升，说明 GRPO 仍然有效地在优化正确的方向</li> </ul> <p>原文并没有做直接去掉 GRPO 过程的 ablation study，所以无法得知原文中 RL 阶段对模型性能的提升幅度。但总体来看，在我的复现工作中，虽然训练过程中 reward 几乎没有可见上升，但最终的 NDCG/HR 改善是真实的。</p> <hr/> <h2 id="总结">总结</h2> <p>完整复现了 MiniOneRec 的完整 pipeline，包括重新训练 RQ-VAE 编码器，并以 Qwen2.5-Instruct-7B 为基座，依次完成了 SFT 与 GRPO 训练。 从 HR 和 NDCG 两项指标来看，复现结果与原文略有差距，但考虑到训练时未严格对齐原文的 batch size、learning rate 等超参数设置，且重训的 RQ-VAE 与原文亦存在差异，这一偏差在合理预期之内。</p> <p>总体而言，复现结果成功验证了 MiniOneRec 的核心主张：将推荐任务与 LLM 对齐训练所构建的新一代生成式推荐系统，确实能取得较为理想的效果；GRPO 对模型性能的提升作用也得到了印证。</p> <p>至于原文关于生成式推荐系统可受益于 scaling law 的主张，由于本次复现仅覆盖 7B 规模，暂无法直接验证。不过，结合原项目 GitHub Issue 中的相关讨论，其他研究者在更小规模模型上的实验结果确实弱于 7B，间接为该主张提供了一定支撑。</p> <hr/> <h2 id="references">References</h2> <p><a href="https://arxiv.org/pdf/2510.24431">MiniOneRec</a></p> <p><a href="https://arxiv.org/pdf/2506.13695">OneRec Technical Report</a></p> <p><a href="https://arxiv.org/pdf/2305.05065">TIGER</a></p>]]></content><author><name></name></author><category term="reproduction"/><summary type="html"><![CDATA[MiniOneRec]]></summary></entry><entry><title type="html">reproduction: Vision Transformer on MNIST</title><link href="https://zhangzfy.github.io/blog/2026/vit/" rel="alternate" type="text/html" title="reproduction: Vision Transformer on MNIST"/><published>2026-05-03T00:00:00+00:00</published><updated>2026-05-03T00:00:00+00:00</updated><id>https://zhangzfy.github.io/blog/2026/vit</id><content type="html" xml:base="https://zhangzfy.github.io/blog/2026/vit/"><![CDATA[<p>对 ViT (<a href="https://openreview.net/pdf?id=YicbFdNTTy">ICLR 2021</a>) 进行了简单复现，在 MNIST 训练集上训练了 50 个 epoch，在测试集上的准确率高于 98%。</p> <p><a href="https://github.com/ZhangZfy/ViT/tree/main">Github link</a></p> <hr/> <h2 id="模型架构">模型架构</h2> <p><img src="/assets/img/posts/2026-05-03-vit/ViT.png" alt="ViT Architecture"/></p> <h3 id="分割图片并线性投影">分割图片并线性投影</h3> <p>以一个 1x28x28 (Channel = 1, H = W = 28)大小的图片为例。</p> <p>设定 patch 尺寸为 4x4，对每一个 patch，我们把它转化成一个 embedding，此处我把 embedding 的维度设置为了和原图片相当的大小，相当于直接拉平了，并没有往高维度投影。即 1x28x28 -&gt; 1x7x7x16，再 reshape 成 1x49x16。通过这样的线性投影变换，1x28x28 大小的图片被转化为 49x16 大小的 token 序列，即共有 49 个 token，每个 token 的 embedding 长度为 16。</p> <p>实际上，ViT 原论文没有用 linear 层来实现线性投影，而是直接用了一个 Conv2d 层，在本质上它们是完全等价的，所以我在这里也直接用一个卷积层实现线性投影逻辑：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>self.conv=nn.Conv2d(
    in_channels=1,  
    out_channels=self.patch_size**2, # 此处可以更改成更高的维度，我在这里没有往更高维度投影。
    kernel_size=self.patch_size,  
    padding=0,
    stride=self.patch_size  
    ) 
</code></pre></div></div> <h3 id="添加-cls-token">添加 cls token</h3> <p>cls token 是一个特殊的 token，它会被拼接到线性投影后的序列的头部，最终用来代表整个图像的全局特征。</p> <h3 id="添加-pos-embedding">添加 pos embedding</h3> <p>pos embedding 是随机初始化的、可学习的 embedding。它直接加到每一个 token 上，形成最终的输入。</p> <h3 id="transformer-encoder">Transformer Encoder</h3> <p>将经过以上步骤处理好的图片数据传入 transformer encoder 层（此处可以叠加多个 encoder）。</p> <h3 id="mlp-head">MLP head</h3> <p>只取 0 号 token 位置的输出，即 cls token 位置的输出，经过 mlp head，得到最终分类结果（类别长度的 logits）。例如，对于一个识别手写数字 0-9 的 ViT，如果我们输入了 5 张手写数字图片（5x1x28x28），最终会得到 5x10 大小的输出，第二维度为该图片为 0-9 对应数字的 logit。</p> <hr/> <h2 id="思考">思考</h2> <p>一个小型的 toy ViT 就可以在 MNIST 数据集上获得不错的效果。但问题在于，CNN 架构在图片分类，以及和视觉相关的各项任务上，表现都已经很好了，而且，由于 CNN 架构自带归纳偏置（局部性、平移等变性），在小规模数据集上，它的表现甚至比 ViT 还要好。</p> <p>那么 ViT 这种架构的意义在何处呢？</p> <p>实际上，ViT 的核心价值，不在于”图片分类比 CNN 准多少”，而在于它把视觉问题纳入了 Transformer 的统一框架。在 ViT 之前，Transformer 架构已经可以说统治了 NLP 领域，但在 CV 领域，CNN 仍然是主要的设计哲学。而 ViT 的出现证明了：<strong>图像也可以当成序列来处理，Transformer 同样适用于视觉</strong>。</p> <p>如此一来，统一架构的多模态大模型才真正成为了可能：视觉和语言用同一套 Transformer 骨干，特征空间可以对齐；同一套代码、同一套训练技巧，可以同时用在视觉和语言任务上，视觉也能开始享受 Scaling law 所带来的大规模预训练红利。</p> <p>一言以蔽之，ViT 的价值不主要是”一个更好的图片分类器”，而是打开了用统一架构处理多种模态的大门，是现在多模态 AI 时代的重要基础之一。</p>]]></content><author><name></name></author><category term="reproduction"/><summary type="html"><![CDATA[对 ViT (ICLR 2021) 进行了简单复现，在 MNIST 训练集上训练了 50 个 epoch，在测试集上的准确率高于 98%。]]></summary></entry><entry><title type="html">My First Post</title><link href="https://zhangzfy.github.io/blog/2026/my-first-post/" rel="alternate" type="text/html" title="My First Post"/><published>2026-04-28T00:00:00+00:00</published><updated>2026-04-28T00:00:00+00:00</updated><id>https://zhangzfy.github.io/blog/2026/my-first-post</id><content type="html" xml:base="https://zhangzfy.github.io/blog/2026/my-first-post/"><![CDATA[<p>Hi! 从今天起，我将开始部署并更新我的个人主页，这里是我和外界对话的窗口，也是我和自己交流的禅地。希望我的个人主页能陪我一起成长，用我的经历去丰富它的内容。也希望来到我主页的访客能从这里获取到想要的信息，更好地了解我，让这个主页为我们创造沟通交流的机会。</p> <p>未来，我将在这里更新我的学术研究、工作经历、日常生活等各种相关内容。</p>]]></content><author><name></name></author><category term="casual"/><summary type="html"><![CDATA[Hi! 从今天起，我将开始部署并更新我的个人主页，这里是我和外界对话的窗口，也是我和自己交流的禅地。希望我的个人主页能陪我一起成长，用我的经历去丰富它的内容。也希望来到我主页的访客能从这里获取到想要的信息，更好地了解我，让这个主页为我们创造沟通交流的机会。]]></summary></entry></feed>